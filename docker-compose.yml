services:
  database:
    image: postgres:15
    env_file: ".env"
    volumes:
      - db-data:/var/lib/postgresql/data

    ports:
      - 5432:5432

  redis:
    image: redis
    ports:
      - 6379:6379
volumes:
  db-data:
